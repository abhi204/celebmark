import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

class InvitesSection extends Component {

    render(){
        return(
            <div>Invites Section</div>
        );
    }
}

export default withRouter(InvitesSection);